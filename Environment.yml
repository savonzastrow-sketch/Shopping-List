name: shopping_list_env
channels:
  - defaults
dependencies:
  - python=3.9 # Explicitly setting a stable Python version
  - pip
  - pip:
    - streamlit
    - pandas
    - gspread
    - gspread-dataframe
